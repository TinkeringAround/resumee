export type TIntro = {
  title: string
  introImage: string
  profileImage: string
}

//=========================================================

export type TIcon = {
  viewport: string
  path: JSX.Element
}
